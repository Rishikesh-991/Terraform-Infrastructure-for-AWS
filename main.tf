terraform {
  required_version = ">= 1.0"
}

# This file is a lightweight placeholder so running `terraform plan` from
# /home/ubuntu/AWS-terraform does not error with "No configuration files".
# Do NOT run applies here.
# terraform-aws-infra/environments (dev/stage/prod) for real work.
